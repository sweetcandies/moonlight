# 注册到 eureka server 配置
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
server:
  port: 8762
spring:
  application:
    name: config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:configs/
  profiles:
    active: main-dev.yml
